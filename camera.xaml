<phone:PhoneApplicationPage
    x:Class="AppSDEM.camera"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="PortraitUp"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="False">

    <!--LayoutRoot è la griglia radice in cui viene inserito tutto il contenuto della pagina-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Button Content="Accesso Fotocamera" Height="72" HorizontalAlignment="Left" Margin="36,90,0,0" Name="camera_button" VerticalAlignment="Top" Width="389" Click="camera_button_click" Background="#BFE4CC79" />
        <Image Height="435" HorizontalAlignment="Left" Margin="36,244,0,0" Name="captured_image" Stretch="Fill" VerticalAlignment="Top" Width="389" />
    </Grid>

</phone:PhoneApplicationPage>